// swift-tools-version:5.5

import PackageDescription

let package = Package(
    name: "ExampleExecutable",
    targets: [
        .executableTarget(
            name: "ExampleExecutableTarget",
            dependencies: []),
    ]
)
